using UnityEngine;

public class DispensorLogic : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    enum DispenseType
    {
        OnDemand,
        OnDestroy,
        Mixed
    }




    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
